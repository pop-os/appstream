#!/bin/sh
# postinst script for appstream-index
set -e

# Force-upgrade the database, in case something has changed
/usr/bin/appstream-index appstream-index --refresh --force >& /dev/null ||:

#DEBHELPER#

exit 0
